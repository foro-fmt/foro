{
	"rules": [
		{
			"on": ".rs",
			"cmd": {
				"io": "uv run python -c 'import sys; s = sys.stdin.read(); sys.stdout.write(s.replace(\"(      )\", \"()\").replace(\"(  \", \"(\").replace(\"   )\", \")\"))'"
			}
		}
	]
}
